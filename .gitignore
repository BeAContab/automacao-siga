# Other
teste-navegador
.venv/
# Dependências de build da interface (Tailwind). Só de build: o app carrega o CSS/JS
# já compilado em src/web_dist/, que É versionado.
web/node_modules/
__pycache__/
*.py[cod]
build/
dist/
# Saídas alternativas do PyInstaller usadas para validar o spec sem sobrescrever dist/
build-webui/
dist-webui/
.browser-profile/
browser-debug-profile/
chrome-live-profile/
edge-live-profile/
logs/
saida/
downloads/
scratch/
testes/*.txt
.env
# Também cobre "COD EMP CNPJ.xlsx" (raiz do projeto) e a planilha-base do NFC-e — dado
# real de cliente (CNPJ), nunca deve ser versionado; cada instalação/checkout mantém sua
# própria cópia (ver DEFAULT_COD_EMPRESA_CNPJ_PATH em src/config.py).
*.xlsx
*.csv
*.log

# Local project notes and chat context
brain/
graphify-out/
.claude/

# Local certificates and secrets
certificado/
certificado/*.pfx
certificado/*.p12
*.pem
*.key
*.crt
*.cer
*.der
*.p7b
*.p7c

# Local databases
*.db
*.sqlite
*.sqlite3
*.sqlite-journal
